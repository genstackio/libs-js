import { s, a } from '../../utils';
import { TasksBlock } from '../../../src';

export default {
    title: 'Organisms/Blocks/TasksBlock',
    component: TasksBlock,
    argTypes: a({}),
};

export const basic = s(TasksBlock, {});
