import ExpandMoreUI from "@material-ui/icons/ExpandMore";

export function ExpandMoreIcon({...props}: ExpandMoreIconProps) {
    return (
        <ExpandMoreUI {...props} />
    );
}

export interface ExpandMoreIconProps {
}

export default ExpandMoreIcon