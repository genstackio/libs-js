import { styling_context_value_short } from './types';

const defaults: Required<styling_context_value_short> = {
    borderShape: 'standard',
    border: 'standard',
    borderColor: 'standard',
    padding: 'standard',
    textColor: 'standard',
    background: 'standard',
    textSize: 'standard',
    thumbnail0Size: 'standard',
    bannerSize: 'standard',
    bannerColor: 'standard',
    logoSize: 'standard',
};

export default defaults;
