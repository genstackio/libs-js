import { createContext } from 'react';

export const StyleConfigContext = createContext({});
export const StyleConfigContextProvider = StyleConfigContext.Provider;
export const StyleConfigContextConsumer = StyleConfigContext.Consumer;
export default StyleConfigContext;
